# Isolated runtime for the pinned RudolfV 2 remote-code checkpoints.
# Install this beside an editable croma checkout; slide2vec is intentionally absent.

torch
torchvision
timm
transformers>=5.14,<6
Pillow>=10.0
