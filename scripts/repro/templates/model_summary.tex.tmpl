\begin{table}[!htbp]
\centering
\footnotesize
\setlength{\tabcolsep}{2pt} % tightened to absorb the per-model citations
\begin{tabular}{llcccl}
\hline
Model & Method & Params & WSIs / tiles & Dim & Pretraining corpus \\
\hline
%%BODY%%
\hline
\end{tabular}
\caption{\textbf{Foundation models evaluated at tile and slide level.} Embedding dimension (Dim) is read from the extracted features and includes any concatenated tokens. Parameter counts and pretraining scale are taken from HEST~\cite{jaume2024} or the source paper/model card; `${\sim}$' denotes an approximate value, `n/d' undisclosed and `n/a' not applicable. Counts are nominal architecture values; for \code{MUSK}, only the vision expert is included. The natural-image control \code{DINOv2-B} was trained on photographs rather than whole-slide images, so pretraining WSIs and tiles are `n/a'. A superscript $\dagger$ denotes a WSI count sourced from PathoROB~\cite{pathorob}. Boldface pretraining corpora include TCGA; \code{H0-mini} and \code{Prost40M} are therefore TCGA-exposed. \code{H-optimus-0} is reported as DINOv2, whereas the recipe for \code{H-optimus-1} is undisclosed and listed as self-supervised learning. PRISM and TITAN aggregate frozen \code{Virchow} and \code{CONCH} tile features, respectively. Prov-GigaPath contributes its tile encoder to PathoROB and its LongNet slide encoder to PANDA. For \code{MOOZY}~\cite{moozy}, we evaluate the slide encoder rather than the patient-level case transformer.}
\label{tab:model-summary}
\end{table}
