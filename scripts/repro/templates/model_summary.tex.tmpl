\begin{table}[!htbp]
\centering
\footnotesize
\setlength{\tabcolsep}{3.5pt}
\begin{tabular}{llcccl}
\hline
Model & Method & Params & WSIs / tiles & Dim & Pretraining corpus \\
\hline
%%BODY%%
\hline
\end{tabular}
\caption{\textbf{Foundation models evaluated.} Embedding dimension (Dim) is read directly from the produced features; where a model concatenates tokens (e.g.\ class~$+$~mean-patch) the dimension reflects the concatenation. Parameter counts and pretraining scale (\#WSIs / \#tiles) are taken from HEST~\cite{jaume2024} where listed and from each model's card or paper otherwise; `${\sim}$' marks approximate or estimated values and `n/d' undisclosed. A superscript $\dagger$ on a \#WSIs count marks a value that is not disclosed on the model card and is taken instead from PathoROB~\cite{pathorob}. \textbf{Pretraining corpora shown in bold include TCGA}, whose overlap with the TCGA-derived benchmarks produces the in-distribution effect quantified in Table~\ref{tab:pretraining-overlap}. \code{H-optimus-0}'s recipe is reported as DINOv2 by HEST~\cite{jaume2024}, whereas \code{H-optimus-1}'s is not separately disclosed and is listed as SSL; \code{H0-mini} is distilled from \code{H-optimus-0} on $43$M tiles from $6{,}093$ public TCGA slides~\cite{h0mini}, making it TCGA-exposed despite its proprietary teacher; \code{Prost40M} is an in-house DINO model~\cite{grisi2026bcr} pretrained on TCGA-PRAD and LEOPARD/RUMC prostate cases. PRISM and TITAN aggregate frozen \code{Virchow} and \code{CONCH} tile features, respectively. Prov-GigaPath appears in both panels: its tile encoder ($1536$-d) is evaluated on PathoROB and its LongNet slide aggregator ($768$-d) on PANDA. For \code{MOOZY}~\cite{moozy} we use the \emph{slide} encoder: a Vision Transformer that aggregates each slide's frozen ViT-S/DINOv2 patch-feature grid (with 2-D ALiBi position bias) into a $768$-d slide embedding, pretrained self-supervised (CLS self-distillation $+$ masked image modelling) and then aligned with multi-task supervision over $333$ public classification and survival tasks. The full \code{MOOZY} model adds a patient-level case transformer that aggregates a patient's slide embeddings into a case embedding, which we do not use here since PANDA is evaluated per slide.}
\label{tab:model-summary}
\end{table}
